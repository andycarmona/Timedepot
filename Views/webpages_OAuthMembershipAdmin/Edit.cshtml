@model TimelyDepotMVC.Models.Admin.webpages_OAuthMembership

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>webpages_OAuthMembership</legend>

        @Html.HiddenFor(model => model.Provider)

        @Html.HiddenFor(model => model.ProviderUserId)

        <div class="display-label">
            @Html.DisplayNameFor(model => model.Provider)
        </div>
        <div class="display-field">
            @Html.DisplayFor(model => model.Provider)
        </div>
        <div class="display-label">
            @Html.DisplayNameFor(model => model.ProviderUserId)
        </div>
        <div class="display-field">
            @Html.DisplayFor(model => model.ProviderUserId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.UserId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.UserId)
            @Html.ValidationMessageFor(model => model.UserId)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
