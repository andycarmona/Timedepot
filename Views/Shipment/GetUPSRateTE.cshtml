@model IEnumerable<TimelyDepotMVC.UPSWrappers.ResultData>
<div class="form-group">
    <label>Choose a UPS rate from list:</label><br/>
        @foreach (var item in Model)
        {
            <label class="pdsa-radiobutton">
                <input id="code"@item.code name="chosenUpsRate" type="radio" value="@item.code" />
                <span> @item.service</span>
               
            </label><br />
        }
    <input class="btn btn-default form-control" type="button" id="btnProcessShipment" value="Process shipment" onclick="processShipment();" />
</div>
