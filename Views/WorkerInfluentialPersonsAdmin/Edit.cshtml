@model TimelyDepotMVC.Models.Admin.WorkerInfluentialPersons

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>WorkerInfluentialPersons</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.categoryID)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.categoryID)
            @Html.ValidationMessageFor(model => model.categoryID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.influentailPersonID)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.influentailPersonID)
            @Html.ValidationMessageFor(model => model.influentailPersonID)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
