@model TimelyDepotMVC.Models.Admin.packagemaster

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>packagemaster</legend>

        @Html.HiddenFor(model => model.PackId)

        <div class="editor-label">
            @Html.LabelFor(model => model.PackName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.PackName)
            @Html.ValidationMessageFor(model => model.PackName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
