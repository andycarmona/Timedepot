@{
    ViewBag.Title = "MainTest";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="divh1">@ViewBag.Title</div>
    @{
        List<string> errorList = null;
        if (ViewBag.errorList != null)
        {
            errorList = ViewBag.errorList;

        }    
    }
<div class="error" style="width: 600px; margin: 5px auto;">
    @ViewBag.ErrorMsg
    @if (errorList != null)
    {
        foreach (var item in errorList)
        {
            @item
        }
    }
</div>
<div style="width: 800px; margin: 5px auto;">
    <div>
        Results here
    </div>
    <div>
        @ViewBag.RateRequest&nbsp;sims to have valida data.<br /><br /><br />
        @ViewBag.Message
    </div>
</div>
