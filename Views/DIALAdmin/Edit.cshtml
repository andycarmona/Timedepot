@model TimelyDepotMVC.Models.Admin.DIAL

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>DIAL</legend>

        @Html.HiddenFor(model => model.DialID)

        <div class="editor-label">
            @Html.LabelFor(model => model.DialPic)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DialPic)
            @Html.ValidationMessageFor(model => model.DialPic)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
