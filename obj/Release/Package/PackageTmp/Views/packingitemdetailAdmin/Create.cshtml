@model TimelyDepotMVC.Models.Admin.packingitemdetail

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>packingitemdetail</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.PackId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.PackId)
            @Html.ValidationMessageFor(model => model.PackId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.itemId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.itemId)
            @Html.ValidationMessageFor(model => model.itemId)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
